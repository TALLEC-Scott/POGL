#version 330 core

in vec3 ourColor;
in vec2 TexCoord;

in float highlight;

out vec4 FragColor;

uniform float delta;
uniform sampler2D ourTexture;

void main() {
	FragColor = texture(ourTexture, TexCoord) * highlight;
}